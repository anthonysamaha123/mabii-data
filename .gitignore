node_modules
.next
out
*.tsbuildinfo
next-env.d.ts
.DS_Store

.env
.env.local
.env*.local

.vercel
.turbo

coverage
.cache
dist

logs
*.log

# Raw fetches are committed for audit trail; transient cache is not
data/cache
data/.tmp

# Survey submissions are private (anonymized aggregates are published separately)
data/survey/submissions
data/survey/dedup
data/survey/velocity.json
data/survey/aggregates
